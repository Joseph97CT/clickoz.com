/* /tools/tools.css?v=2
   Clickoz Tools page – centered hero + centered sections + improved bottom SEO/design
   Works with existing HTML structure (tools-shell, tools-hero, chips, tool-section, lanes, faq-grid, seo-boost)
*/

:root{
  --tool-radius: 22px;
  --tool-radius-sm: 16px;
  --tool-border: rgba(255,255,255,.10);
  --tool-border-2: rgba(255,255,255,.14);
  --tool-bg: rgba(255,255,255,.04);
  --tool-bg-2: rgba(255,255,255,.06);
  --tool-shadow: 0 22px 60px rgba(0,0,0,.45);
  --tool-shadow-soft: 0 16px 40px rgba(0,0,0,.32);
  --tool-text-dim: rgba(242,242,255,.74);
  --tool-text-dimmer: rgba(242,242,255,.60);
  --tool-title: rgba(255,255,255,.94);
  --tool-glow: rgba(var(--accent-rgb),.20);
  --tool-glow2: color-mix(in srgb, var(--accent2) 18%, transparent);
}

.tools-shell{
  padding-top: 26px;
}

/* ✅ remove breadcrumb row visually even if HTML remains */
.crumbs{
  display:none !important;
}

/* -----------------------------
   HERO (centered)
------------------------------ */
.tools-hero{
  position: relative;
  border: 1px solid var(--tool-border);
  background: radial-gradient(1200px 700px at 15% -10%, var(--tool-glow), transparent 55%),
              radial-gradient(900px 600px at 105% 10%, var(--tool-glow2), transparent 55%),
              linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
  box-shadow: var(--tool-shadow);
  border-radius: var(--tool-radius);
  padding: 28px 22px 22px;
  overflow: hidden;
}

.tools-hero::before{
  content:"";
  position:absolute; inset:-2px;
  pointer-events:none;
  background:
    radial-gradient(800px 400px at 50% 0%, rgba(255,255,255,.08), transparent 60%),
    linear-gradient(180deg, rgba(255,255,255,.10), transparent 35%);
  opacity:.55;
}

.tools-hero-top{
  position: relative;
  display:flex;
  gap:18px;
  align-items:center;
  justify-content:center;
  text-align:center;
  margin-bottom: 12px;
  flex-wrap: wrap;
}

/* Make the left block full width and centered */
.tools-hero-top > div:first-child{
  flex: 1 1 820px !important;
  max-width: 980px;
}

.tools-title{
  margin: 0 0 10px;
  font-weight: 1000;
  letter-spacing: -.02em;
  line-height: 1.06;
  font-size: clamp(34px, 5vw, 56px);
  color: var(--tool-title);
  text-shadow: 0 10px 40px rgba(0,0,0,.35);
}

.tools-sub{
  margin: 0 auto 14px;
  max-width: 860px;
  font-size: clamp(16px, 2.1vw, 18px);
  line-height: 1.55;
  color: var(--tool-text-dim);
}

.tools-trust-row{
  display:flex;
  flex-wrap:wrap;
  justify-content:center;
  gap:10px;
  margin: 12px auto 2px;
}

.tools-trust-row span{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  min-height:38px;
  padding: 9px 12px;
  border-radius:999px;
  border:1px solid rgba(var(--accent-rgb), .20);
  background: rgba(var(--accent-rgb), .075);
  color: rgba(255,255,255,.86);
  font-size: 13px;
  font-weight: 900;
}

/* Keep old search wrapper but we will visually re-home it under chips */
.tools-search{
  flex: 1 1 100% !important;
  max-width: 720px !important;
  width: 100%;
  display:flex;
  justify-content:center;
  margin: 0 auto;
}

/* Chips */
.chips{
  position: relative;
  display:flex;
  gap:10px;
  flex-wrap: wrap;
  justify-content:center;
  margin-top: 14px;
  margin-bottom: 14px;
}

.chip{
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.18);
  color: rgba(255,255,255,.86);
  padding: 10px 14px;
  border-radius: 999px;
  font-size: 13px;
  font-weight: 800;
  letter-spacing: .02em;
  cursor:pointer;
  user-select:none;
  transition: transform .12s ease, background .15s ease, border-color .15s ease, box-shadow .15s ease;
  box-shadow: 0 10px 26px rgba(0,0,0,.24);
}

.chip:hover{
  transform: translateY(-1px);
  border-color: var(--tool-border-2);
}

.chip.active{
  border-color: rgba(255,255,255,.20);
  background: linear-gradient(180deg, rgba(255,255,255,.10), rgba(255,255,255,.05));
  box-shadow: 0 14px 34px rgba(0,0,0,.30);
}

/* Search input – centered under chips */
.tools-hero-row.tools-search{
  order: 3;
  margin-top: 2px;
}

.search{
  width: min(720px, 100%);
  border-radius: 14px;
  padding: 14px 14px;
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.22);
  color: rgba(255,255,255,.92);
  outline: none;
  box-shadow: 0 14px 38px rgba(0,0,0,.34);
  transition: border-color .15s ease, transform .12s ease, box-shadow .15s ease;
}

.search::placeholder{
  color: rgba(242,242,255,.55);
}

.search:focus{
  border-color: rgba(255,255,255,.22);
  box-shadow: 0 16px 44px rgba(0,0,0,.38);
  transform: translateY(-1px);
}

.tools-empty{
  margin-top: 16px;
}

.tools-empty-suggestions{
  display:flex;
  flex-wrap:wrap;
  justify-content:center;
  gap:8px;
  margin-top:12px;
}

.tools-empty-suggestions button{
  min-height:34px;
  padding:7px 10px;
  border-radius:999px;
  border:1px solid rgba(var(--accent-rgb), .22);
  background:rgba(var(--accent-rgb), .08);
  color:rgba(255,255,255,.90);
  font:inherit;
  font-size:12px;
  font-weight:900;
  cursor:pointer;
  transition:transform .14s ease, border-color .14s ease, background .14s ease;
}

.tools-empty-suggestions button:hover,
.tools-empty-suggestions button:focus-visible{
  transform:translateY(-1px);
  border-color:rgba(var(--accent-rgb), .40);
  background:rgba(var(--accent-rgb), .13);
}

/* Give the hero a nice bottom spacing */
.tools-hero + .tool-sections{
  margin-top: 18px;
}

/* -----------------------------
   CATEGORY SECTIONS (centered header)
------------------------------ */
.tool-sections{
  margin-top: 18px;
  display:flex;
  flex-direction: column;
  gap: 18px;
}

.tool-section{
  border: 1px solid var(--tool-border);
  background: rgba(255,255,255,.03);
  border-radius: var(--tool-radius);
  box-shadow: var(--tool-shadow-soft);
  padding: 22px 18px 18px;
}

.section-head{
  display:flex;
  gap: 12px;
  align-items: flex-start;
  justify-content: center;
  text-align:center;
  flex-wrap: wrap;
  margin-bottom: 14px;
}

.section-head > div:first-child{
  max-width: 900px;
}

.section-kicker{
  display:flex;
  align-items:center;
  justify-content:center;
  gap: 10px;
  margin-bottom: 6px;
}

.section-ico{
  width: 44px;
  height: 44px;
  border-radius: 14px;
  display:flex;
  align-items:center;
  justify-content:center;
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.18);
  box-shadow: 0 12px 26px rgba(0,0,0,.25);
  font-size: 20px;
}

.section-name{
  margin: 0;
  font-weight: 1000;
  letter-spacing: -.02em;
  color: var(--tool-title);
  font-size: clamp(24px, 3.4vw, 34px); /* ✅ bigger titles */
  line-height: 1.1;
}

.section-desc{
  margin: 0 auto;
  max-width: 820px;
  color: var(--tool-text-dim);
  line-height: 1.55;
  font-size: 15px;
}

.section-count{
  margin-top: 2px;
  padding: 10px 12px;
  border-radius: 999px;
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.18);
  color: rgba(255,255,255,.82);
  font-size: 13px;
  font-weight: 800;
  align-self: center;
}

/* cards */
.cards-grid{
  display:grid;
  gap: 12px;
  grid-template-columns: repeat(12, 1fr);
  margin-top: 14px;
}

.card{
  grid-column: span 6;
  border: 1px solid var(--tool-border);
  background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
  border-radius: var(--tool-radius-sm);
  padding: 14px 14px 12px;
  text-decoration: none;
  color: inherit;
  box-shadow: 0 14px 34px rgba(0,0,0,.30);
  transition: transform .12s ease, border-color .15s ease, box-shadow .15s ease;
  position: relative;
  overflow:hidden;
}

.card::before{
  content:"";
  position:absolute; inset:0;
  background: radial-gradient(500px 220px at 25% 0%, rgba(255,255,255,.10), transparent 60%);
  opacity:.55;
  pointer-events:none;
}

.card:hover{
  transform: translateY(-2px);
  border-color: var(--tool-border-2);
  box-shadow: 0 20px 48px rgba(0,0,0,.38);
}

.card-top{
  display:grid;
  grid-template-columns: 52px minmax(0, 1fr);
  gap: 12px;
  align-items:center;
  position: relative;
}

.card-ico{
  width: 52px;
  height: 52px;
  border-radius: 14px;
  border: 1px solid rgba(var(--accent-rgb), .28);
  background:
    radial-gradient(circle at 35% 20%, rgba(255,255,255,.18), transparent 36%),
    rgba(var(--accent-rgb), .10);
  display:flex;
  align-items:center;
  justify-content:center;
  box-shadow: 0 12px 24px rgba(0,0,0,.22);
  font-size: 22px;
}

.card h3{
  margin: 0 0 6px;
  font-size: clamp(17px, 1.8vw, 20px);
  font-weight: 900;
  color: rgba(255,255,255,.92);
  letter-spacing: .025em;
  text-transform: uppercase;
  line-height:1.15;
}

.card p{
  margin: 0;
  color: var(--tool-text-dimmer);
  font-size: 15.5px;
  line-height: 1.58;
}

.tool-cta{
  display:inline-flex;
  align-items:center;
  gap: 8px;
  margin-top: 12px;
  font-weight: 900;
  font-size: 13px;
  color: rgba(255,255,255,.88);
  position: relative;
}

.tool-cta::after{
  content:"→";
  opacity: .9;
  transform: translateX(0);
  transition: transform .12s ease;
}

.card:hover .tool-cta::after{
  transform: translateX(3px);
}

/* Responsive cards */
@media (max-width: 920px){
  .card{ grid-column: span 12; }
  .tools-hero{ padding: 24px 16px 18px; }
}

/* -----------------------------
   BOTTOM SECTIONS (design + SEO)
   Applies to:
   - .hero-box
   - .lanes + .lane
   - .faq-grid + .faq
   - .seo-boost
------------------------------ */
.hero-box{
  border: 1px solid var(--tool-border);
  background:
    radial-gradient(900px 520px at 20% 0%, rgba(255,255,255,.08), transparent 60%),
    linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
  border-radius: var(--tool-radius);
  box-shadow: var(--tool-shadow-soft);
  padding: 22px 18px 18px;
}

.section-title{
  font-weight: 1000;
  letter-spacing: -.02em;
  color: rgba(255,255,255,.94);
  font-size: clamp(22px, 3.2vw, 34px);
  line-height: 1.12;
  text-align:center; /* ✅ center */
}

.section-sub{
  color: var(--tool-text-dim);
  line-height: 1.6;
  text-align:center; /* ✅ center */
  max-width: 980px;
  margin-left:auto;
  margin-right:auto;
}

.lanes{
  margin-top: 14px;
  display:grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 12px;
}

.lane{
  grid-column: span 6;
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.16);
  border-radius: var(--tool-radius-sm);
  padding: 14px 14px 12px;
  box-shadow: 0 12px 30px rgba(0,0,0,.24);
  position: relative;
  overflow:hidden;
}

.lane::before{
  content:"";
  position:absolute; inset:0;
  background: radial-gradient(520px 220px at 20% 0%, rgba(255,255,255,.10), transparent 62%);
  opacity:.55;
  pointer-events:none;
}

.lane-head{
  display:flex;
  align-items:center;
  gap: 10px;
  margin-bottom: 6px;
  justify-content:center; /* ✅ center icon+title */
  text-align:center;
}

.lane-ico{
  width: 42px;
  height: 42px;
  border-radius: 14px;
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.18);
  display:flex;
  align-items:center;
  justify-content:center;
  box-shadow: 0 12px 24px rgba(0,0,0,.22);
  font-size: 20px;
}

.lane h3{
  margin: 0;
  font-weight: 1000;
  letter-spacing: -.01em;
  color: rgba(255,255,255,.92);
  font-size: 16px;
  line-height: 1.15;
}

.lane p{
  margin: 0;
  color: var(--tool-text-dim);
  font-size: 13.5px;
  line-height: 1.6;
  text-align:center; /* ✅ center */
}

@media (max-width: 920px){
  .lane{ grid-column: span 12; }
}

/* FAQ redesign */
.faq-grid{
  margin-top: 14px;
  display:grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 12px;
}

.faq{
  grid-column: span 6;
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.16);
  border-radius: var(--tool-radius-sm);
  padding: 14px 14px 12px;
  box-shadow: 0 12px 30px rgba(0,0,0,.22);
}

.faq h3{
  margin: 0 0 8px;
  font-weight: 1000;
  font-size: 15px;
  color: rgba(255,255,255,.92);
  letter-spacing: -.01em;
  text-align:center; /* ✅ center */
}

.faq p{
  margin: 0;
  color: var(--tool-text-dim);
  line-height: 1.6;
  font-size: 13.5px;
  text-align:center; /* ✅ center */
}

@media (max-width: 920px){
  .faq{ grid-column: span 12; }
}

/* SEO boost block – more premium + internal links like chips */
.seo-boost{
  margin-top: 14px;
  border: 1px solid rgba(255,255,255,.14);
  background: radial-gradient(900px 420px at 25% 0%, rgba(255,255,255,.10), transparent 60%),
              rgba(0,0,0,.18);
  border-radius: var(--tool-radius-sm);
  padding: 16px 14px 14px;
  box-shadow: 0 16px 40px rgba(0,0,0,.28);
  text-align:center; /* ✅ */
}

.seo-boost h3{
  margin: 0 0 8px;
  font-weight: 1000;
  letter-spacing: -.01em;
  color: rgba(255,255,255,.94);
  font-size: 16px;
}

.seo-boost p{
  margin: 0 auto 10px;
  max-width: 980px;
  color: var(--tool-text-dim);
  font-size: 13.5px;
  line-height: 1.7;
}

.seo-boost-links{
  display:flex;
  gap: 10px;
  flex-wrap: wrap;
  justify-content:center;
  margin-top: 10px;
}

.seo-boost-links a{
  text-decoration:none;
  color: rgba(255,255,255,.90);
  border: 1px solid var(--tool-border);
  background: rgba(0,0,0,.18);
  padding: 10px 12px;
  border-radius: 999px;
  font-weight: 900;
  font-size: 12.5px;
  transition: transform .12s ease, border-color .15s ease, box-shadow .15s ease;
  box-shadow: 0 12px 26px rgba(0,0,0,.24);
}

.seo-boost-links a:hover{
  transform: translateY(-1px);
  border-color: var(--tool-border-2);
  box-shadow: 0 16px 34px rgba(0,0,0,.30);
}

/* minor spacing polish */
.section.container[aria-label="Tools value and use cases"],
.section.container[aria-label="Tools FAQ"]{
  padding-top: 14px;
}

/* Accessibility focus states */
.chip:focus,
.color-option:focus,
.search:focus,
.card:focus,
.seo-boost-links a:focus{
  outline: 2px solid rgba(255,255,255,.22);
  outline-offset: 3px;
}

/* Advanced tools directory finish */
:root {
  --tool-glow1: rgba(var(--accent-rgb), .18);
  --tool-glow2: color-mix(in srgb, var(--accent2) 18%, transparent);
}

.tools-hero,
.tool-section,
.lane,
.faq,
.seo-boost {
  border-color: rgba(var(--accent-rgb), .18);
}

.tools-hero {
  padding: clamp(28px, 4vw, 42px);
  background:
    radial-gradient(1200px 720px at 15% -10%, rgba(var(--accent-rgb), .18), transparent 55%),
    radial-gradient(900px 620px at 105% 10%, color-mix(in srgb, var(--accent2) 18%, transparent), transparent 55%),
    linear-gradient(180deg, rgba(255,255,255,.065), rgba(255,255,255,.028));
}

.tools-title {
  text-wrap: balance;
}

.tools-chips .chip,
#toolsChips .chip,
.section-count {
  border-color: rgba(var(--accent-rgb), .18);
}

#toolsChips .chip.active {
  border-color: rgba(var(--accent-rgb), .34);
  background: linear-gradient(180deg, rgba(var(--accent-rgb), .18), rgba(255,255,255,.055));
}

.tool-section {
  position: relative;
  overflow: hidden;
  background:
    linear-gradient(135deg, rgba(255,255,255,.085), rgba(255,255,255,.028)),
    radial-gradient(720px 320px at 12% 0%, rgba(var(--accent-rgb), .12), transparent 68%),
    rgba(8,16,30,.80);
}

.tool-section::after {
  content: "";
  position: absolute;
  left: 18px;
  right: 18px;
  top: 0;
  height: 3px;
  border-radius: 0 0 999px 999px;
  background: linear-gradient(90deg, transparent, rgb(var(--accent-rgb)), transparent);
  opacity: .62;
}

.tool-section > * {
  position: relative;
}

.tool-card,
.card {
  min-height: 220px;
  border-color: rgba(var(--accent-rgb), .16);
  transition: transform .16s ease, border-color .16s ease, box-shadow .16s ease;
}

.tool-card:hover,
.card:hover {
  transform: translateY(-2px);
  border-color: rgba(var(--accent-rgb), .34);
}

.tool-icon,
.card-icon,
.lane-ico {
  border-color: rgba(var(--accent-rgb), .20);
  background:
    radial-gradient(circle at 30% 24%, rgba(var(--accent-rgb), .32), transparent 58%),
    rgba(255,255,255,.05);
}

.lane,
.faq,
.seo-boost {
  background:
    linear-gradient(135deg, rgba(var(--accent-rgb), .08), rgba(255,255,255,.035)),
    rgba(0,0,0,.18);
}

.tools-command {
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(320px, .9fr);
  gap: 16px;
  margin-top: 18px;
}

.tools-command-main,
.tools-command-side a,
.tools-finish {
  border: 1px solid rgba(var(--accent-rgb), .20);
  border-radius: 24px;
  background:
    linear-gradient(135deg, rgba(255,255,255,.085), rgba(255,255,255,.028)),
    radial-gradient(680px 300px at 12% 0%, rgba(var(--accent-rgb), .14), transparent 68%),
    rgba(8,16,30,.78);
  box-shadow: 0 24px 80px rgba(0,0,0,.34), inset 0 1px 0 rgba(255,255,255,.08);
}

.tools-command-main {
  min-height: 260px;
  padding: 24px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.tools-command-main h2 {
  margin: 0;
  color: #fff;
  font-size: clamp(32px, 4vw, 56px);
  line-height: 1.04;
  letter-spacing: 0;
}

.tools-command-main p:not(.guide-kicker) {
  max-width: 740px;
  color: rgba(248,252,255,.72);
  line-height: 1.7;
}

.tools-command-side {
  display: grid;
  gap: 12px;
}

.tools-command-side a {
  min-height: 92px;
  padding: 18px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  text-decoration: none;
  color: inherit;
  transition: transform .16s ease, border-color .16s ease, box-shadow .16s ease;
}

.tools-command-side a:hover {
  transform: translateY(-2px);
  border-color: rgba(var(--accent-rgb), .34);
  box-shadow: 0 24px 70px rgba(0,0,0,.38), 0 0 54px rgba(var(--accent-rgb), .12);
}

.tools-command-side strong {
  color: #fff;
  font-size: 18px;
}

.tools-command-side span {
  margin-top: 6px;
  color: rgba(248,252,255,.66);
  line-height: 1.5;
}

.tools-finish {
  margin-top: 16px;
  padding: 20px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(300px, .7fr);
  gap: 16px;
  align-items: center;
  text-align: left;
}

.tools-finish h3 {
  margin: 0;
  color: #fff;
  font-size: clamp(24px, 3vw, 36px);
  line-height: 1.1;
}

.tools-finish p:not(.guide-kicker) {
  color: rgba(248,252,255,.68);
  line-height: 1.65;
}

@media (max-width: 920px) {
  .tools-command,
  .tools-finish {
    grid-template-columns: 1fr;
  }
}

